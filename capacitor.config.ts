import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rxjs-state-mgt-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
